@Library('sharedlib@main')_

pipeline{
    agent any
    stages{
        stage('Dump Tomcat Threads'){
            steps{
                script{
                    ok = dumpTomcatThreads(
                        hostname = "${hostname}"
                    )
                }
            }
        }
    }
}